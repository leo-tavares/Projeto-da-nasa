import { createElement } from "../../utils/elements";

const btnAnterior = createElement("button", "button_anterior", ["button_anterior"]);
const btnPosterior = createElement("button", "button_posterior", ["button_posterior"]);


export {
    btnAnterior,
    btnPosterior
}